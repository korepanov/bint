0) не везде читается str 

1) 
int c;
print(str(c));

2)
-переменная

4)
не везде читается len, индекс, переменная[индекс1:индекс2], is_letter, is_digit, reg_find

5)
не вычисляется индекс внутри выражения переменная[индекс]

10) разрешение конфликтов одинаковых имен разных файлов import 

12) в функцию нельзя передать выражение 

13) Возможно ли вернуть bool как сравнение? Как выражение с OR и XOR, например?

15) Добавить возможность вложенных включений 

16) Добавить к core внутри stdlib новые файлы  

17) Добавить возможность комментариев 

18) Булевы присваивания типа t = (a < b)

19) Проверить функции, которые вызывают друг друга 

20) Если положить в стек стек и некоторую строку, то что случит флагом конца стека?

21) Две метки подряд не могут идти без хотя бы одной команды между ними - добавить этот факт в валидатор 

22) Области видимости меток 

23) Добавить рекурсию. Установить максимальный размер стека рекурсии

24) (!) Обработка меток - nop после метки 

25) вычислить максимальную длину строки и поставить ограничитель в валидаторе 
