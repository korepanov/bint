2. Каким образом произойдет импорт, если импортировать не только функции, но и глобальные переменные?
3. Реализовать срез строки 
3. все новые ключевы слова добавлять в функцию serviceTools.IsKeyWord 
13. CLEAR в рекурсии 
14. разопределение переменных в результате рекурсии
15. Добавить функцию executable(), возвращающую путь к бинарнику
16. __defineVar проверяет, кончается ли место под таблицу переменных перед дефайном, но не проверяет, кончается ли место под таблицы строк и стеков 
17. сделать внутренний сдвиг стеков
вызывать функцию по выделению дополительной памяти, если это необходимо 
18. пушить в стек строки и стеки userPush 
правильно сдвигать их в __shiftInternalStacks 
19.
userPush и userPop
нельзя шагать по stackSize, потому что в стеке могут быть строки 
21.
попать end  
22.
в string можно запопать все что угодно, кроме стека - реализовать 
23.
реализовать пуш не только переменной, но и значения 
24.
userPop должен менять stackPointer 
25.
проверить userPop на string
