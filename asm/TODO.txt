2. унарные операции от выражения не должны пропускаться, если неправильно расставлены скобки  
3. exit(s), где s - строковая переменная, допускается динамическими валидатором 
4. exit(a), где а - переменная типа int, не компилируется 

