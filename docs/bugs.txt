1) 
int c;
print(str(c));

2)
-переменная

3)
print(10) - работает 

4)
не везде читается len, индекс, переменная[индекс1:индекс2], is_letter, is_digit

5)
не вычисляется индекс внутри выражения переменная[индекс]

6)
проблема с кавычками 

7)
функция может быть вызвана в строке единственный раз

8)
при доставании данных с верхушки стека нет контроля типов 

9)
при присваивании одного стека другому и доставании данных из одного из них, данные достаются из обоих (?)

10)
(!)
no such mark указывает на ту строку, где останавливается поиск, а не на ту, где содержится вызов goto
