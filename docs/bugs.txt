6)
проблема с кавычками 

11) is_letter и is_digit возвращают bool с маленькой буквы 

12) не всегда правильно показывается номер строки, в которой происходит ошибка (сдвиг на единицу)

13) невозможно напечатать ключевое слово через print, поскольку слова в кавычках также преобразуются через benv

14) (!) слово, содержащее в своем составе ключевое слово, может быть распознано как ключевое слово. Например, переменная length может быть распознана как операция len

15) (!) слова в кавычках могут быть распознаны как ключевые слова

16) (!) если объявить функцию с именем T и использовать внутри нее операцию NOT, то последняя буква слова NOT будет распознана как вызов функции модулем recurs.b

16) (!) return_type="" воспринимается как return

17) невозможно передать в функцию символ ")" напрямую 

18) невозможно передать в функцию ops символ "}" через временную переменную 

21) в string возможно достать stack 

22) если переменная не объявлена, то печатается ее имя, а не сообщение о соответствующей ошибке 

23) неправильно обрабатывается ";" внутри кавычек 

