1) 
int c;
print(str(c));

2)
-переменная

3)
print(10) - работает 

4)
не везде читается len, индекс, переменная[индекс1:индекс2], is_letter, is_digit

5)
не вычисляется индекс внутри выражения переменная[индекс]

6)
проблема с кавычками 

8)
при доставании данных с верхушки стека нет контроля типов 

9) 
при отсутствии инициализации переменной в ней лежит var_val

10) разрешение конфликтов одинаковых имен разных файлов import 

11) is_letter и is_digit возвращают bool с маленькой буквы 
