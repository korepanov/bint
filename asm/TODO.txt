3. все новые ключевы слова добавлять в функцию serviceTools.IsKeyWord 
8. Полноценная работа с файлами
8. доделать функцию $open_file для работы с переменными 
9. Сделать и обернуть функцию $close_f(file)
10. Доделать все функции по работе с файлами. 
10. Поправить комментарии в stdlib/files.b
11. Проблема утечки памяти при разопределении переменной (!)
Разнести UNDEFINE на две функции: собственно UNDEFINE и CLEAR (!)
нужно делать UNDEFINE $func_res0 
CLEAR в цикле и рекурсии 
12. Починить программу с генерацией простых чисел
Обрабытвая if, elseif и else нужно обработать все return, разопределяя соответствующие переменные  
Менять if и for в internal 
