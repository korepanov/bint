1) строка не передается в функцию напрямую 
5) два и более return  
6) возвращение выражения в return 
7) функции с именем, в которое входит имя другой функции, не транслируется 
8) вызов функции внутри функции 
