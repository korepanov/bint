6)
проблема с кавычками 

11) is_letter и is_digit возвращают bool с маленькой буквы 

12) не всегда правильно показывается номер строки, в которой происходит ошибка (сдвиг на единицу)

13) невозможно напечатать ключевое слово через print, поскольку слова в кавычках также преобразуются через benv

