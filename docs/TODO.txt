-7) проверить exists в режиме защиты 
-6) разобраться с тем, какой путь подавать exists в basm и в исполняемом файле 
-5) обработчик return со скобками 
-4) -переменная
-3) обработчик арифметики со скобками 
-2) Добавить возможность вложенных включений  
-1) тестировать вадилатор с импортом других файлов 

0) не везде читается str 

1) 
int c;
print(str(c));

4)
не везде читается len, индекс, переменная[индекс1:индекс2], is_letter, is_digit, reg_find, exists

добавляя соответствующие модули обработки,необходимо добавить их и для обработки (уже!) трассированной программы 
5)
не вычисляется индекс внутри выражения переменная[индекс]

10) разрешение конфликтов одинаковых имен разных файлов import 

12) в функцию нельзя передать выражение 

13) проблема с выражением k = sum((5+1), (6-1))

14) добавить возможность continue для for цикла 

15) чертовщина с абсолютными и относительными путями 

16) обработка неверного регулярного выражения в reg_find валидатором


