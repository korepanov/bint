1. Анализатор должен допускать условный return, если возврат происходит в любом случае 
2. Пофиксить утечки памяти, связанные с неправильным UNDEFINE внутри цикла for. В частности в функции, внутри которой цикл, внутри которого цикл
3. Обработчик return со скобками
4. Обработчик арифметики со скобками 
5. -переменная
6. слова в кавычках могут быть распознаны как ключевые слова
7. is_letter и is_digit возвращают bool с маленькой буквы 
8. не всегда правильно показывается номер строки, в которой происходит ошибка (сдвиг на единицу)
9. невозможно напечатать ключевое слово через print, поскольку слова в кавычках также преобразуются через benv
10. невозможно передать в функцию ops символ "}" через временную переменную 
11. ключевые слова должны отсутствовать внутри имени переменных (end тоже ключевое слово) 
12. возможность применить слайс к слайсу 
13. валидатор должен смотреть, что пользовательские функции принимают аргументы правильного типа 
14. проверка валидатором более одного : в слайсе 
15. Деление на нуль и целочисленное деление с отрицательными числами должны быть выявлены валидатором на этапе компиляции, если это возможно 
