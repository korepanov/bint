2. Не передается символ "\"" в качестве аргумента функции 
2. Не передается выражение в функцию. Например, (counter - 1) не передается в функцию, принимающую аргумент типа int 
2. не обрабатывается выражение sum2(sum(1, 2), 3) также, как и sum(sum(1, 2), 3)
3. Не возвращается return reverse(res); 
4. Проверить различные комбинации вызовов функции в функции 
5. Не обрабатывается функция, вложенная сама в себя 
