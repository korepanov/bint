int sum(int a, int b){
	return (a + b);
};

int res;

int sum2(){
	return (2 + 3);
};

float subtitution(float first, float second){
	return (first - second);
};

res = (sum(1, 2) + sum(2, 3));
string buf;
buf = str(res);
print(buf);
print("\n");
